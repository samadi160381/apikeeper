{
  "name": "api-key-system",
  "version": "1.0.0",
  "description": "API key management system with usage-based billing and rate limiting",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "psql \"$DATABASE_URL\" -f src/db/schema.sql"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "ioredis": "^5.4.1",
    "dotenv": "^16.4.5",
    "stripe": "^16.8.0",
    "nanoid": "^5.0.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
